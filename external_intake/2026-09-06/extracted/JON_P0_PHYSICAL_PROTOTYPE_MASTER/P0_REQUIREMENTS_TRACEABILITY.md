# P0_REQUIREMENTS_TRACEABILITY

ORIGINAL CONCEPT -> R4.1 -> P0 -> TEST CONFIGURATION -> RAW DATA -> EVIDENCE -> MANUS -> MBD/FE

The stable numerals 100-210 are the controlled traceability vocabulary from the V7 design package. The physical prototype does not alter R4.1.

See `P0_REQUIREMENTS_TRACEABILITY.csv` for the machine-readable mapping.
