# P0_TEST_MATRIX

The controlled sequence is: mass properties -> contact/interface -> absorber -> joints/lock -> integrated functional -> controlled dynamic.

See `P0_TEST_MATRIX.csv` for complete procedures, instrumentation, repeat counts and evidence outputs.
